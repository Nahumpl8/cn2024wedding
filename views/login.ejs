<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Claudia & Nahum</title>
    <style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        html{
            font-size: 10px;
        }
        body{
            background-color: #f5f5f5;
        }
        .principal{
            display: grid;
            grid-template-columns: 1fr 1fr;
            height: 100vh;
        }
        .photo img{
            width: 100%;
            height: 100vh;
            object-fit: cover;
        }

        .access_page{
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        .form_access{
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            padding: 5%;
        }
        .form_access form{
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: space-evenly;
            height: 20vh;
        }

        .title{
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 5%;
        }
        .title h1{
            font-family: Georgia, 'Times New Roman', Times, serif;
            font-size: 3rem;
            color: #c0a080;
        }
        .title h2{
            font-family: Georgia, 'Times New Roman', Times, serif;
            font-size: 1.8rem;
            color: #c0a080;
        }

        #nombre,
        #contraseña {
            border: none;
            border-bottom: 2px solid #c0a080;
            background-color: transparent;
            padding: 10px 20px;
            font-family: Georgia, 'Times New Roman', Times, serif;
            text-align: center;
            color: rgb(96, 118, 96);
            font-size: 1.8rem;
            transition: border-bottom 0.3s, color 0.3s; 
            min-width: 250px;
        }

        #nombre:focus,
        #contraseña:focus {
            border: none;
            border-bottom: 2px solid #c0a080;
            background-color: transparent; 
            color: rgb(96, 118, 96); 
        }
        #nombre:active,
        #contraseña:active {
            border: none;
            background-color: transparent; 
            color: rgb(96, 118, 96); 
        }
        

        #nombre::-webkit-input-placeholder,
        #contraseña::-webkit-input-placeholder {
            border: none;
            color: rgba(96, 118, 96, 0.4);
        }

        

        button{
            margin-top: 30px;
            border: none;
            border-radius: 5px;
            background-color: #c0a080;
            color: #f5f5f5;
            padding: 10px;
        }
        .form_access h2{
            display: none;
        }


        @media screen and (max-width: 720px) {
            .principal{
                display: grid;
                grid-template-columns: 1fr;
                height: 100vh;
            }
            .photo_web{
                position: relative;
            }
            .photo_web img{
                filter: brightness(.5);
                object-position: bottom;
            }
            .title{
                position: absolute;
                top: 15%;
            }
            .title h1{
                font-size: 3.6rem;
                color: #f5f5f5;
            }
            .title h2{
                font-size: 2.4rem;
                margin-top: 15px;
                color: #f5f5f5;
            }
            
            .title h1, h2{
                text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            }
            .form_access{
                position: absolute;
                background-color: #f5f5f5bf;
                bottom: 28%;  
                border-radius: 5px;  
                width: 80%;   
                bottom: 25%;
            }
            .form_access h2{
                display: block;
                width: 80%;
                text-align: center;
                color: #c0a080;
                text-shadow: none;
            }
        }
    </style>
</head>
<body>
    <section class="principal">
        <div class="photo_web">
            <div class="photo">
                <img src="/images/NahunClau_Preboda-19.jpg" alt="">
            </div>
        </div>
        <div class="access_page">
            <div class="title">
                <h1>Claudia & Nahum</h1>
                <h2>19 · Octubre · 2024</h2>
            </div>
            <div class="form_access">
                <h2>Ingresa con tu usuario y contraseña para ver tu invitación</h2>
                <form action="/login" method="post">
                    <label for="nombre"></label>
                    <input type="text" id="nombre" name="nombre" list="opcionesNombre" placeholder="Selecciona tu nombre" required>
                    <datalist id="opcionesNombre">
                        <% opcionesNombre.forEach(function(nombre) { %>
                          <option value="<%= nombre %>">
                        <% }); %>
                    </datalist>
                    
                    <label for="contraseña"></label>
                    <input type="password" id="contraseña" name="contraseña" placeholder="Contraseña" required>
            
                    <button type="submit">Ver invitación</button>

                </form>
            </div>
        </div>
    </section>


    <script>
        const nombreInput = document.getElementById('nombre');
        const opcionesNombre = document.getElementById('opcionesNombre').options;
      
        nombreInput.addEventListener('input', function () {
          const filtro = nombreInput.value.toLowerCase();
          for (let i = 0; i < opcionesNombre.length; i++) {
            const opcion = opcionesNombre[i].value.toLowerCase();
            if (opcion.includes(filtro)) {
              opcionesNombre[i].style.display = 'block';
            } else {
              opcionesNombre[i].style.display = 'none';
            }
          }
        });
    </script>
</body>
</html>
